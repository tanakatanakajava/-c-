
メモリの番号の表示
%pを使えばよし

#include<stdio.h>
int main(void){
  int i;
  printf("%p",&i);
  return 0;
  }
  
  &つき変数とは、アドレスを求める演算子のこと
  
  ポインタとは、アドレスの値を記憶する変数のこと
  
  １．ポインタ型
  int ,doubleみたいなもん
  
  2.ポインタ地
  ポインタ型で扱える数値（アドレス）
  
  3.ポインタ変数
  ポインタが出宣言された、ポインタがを記憶できる変数のこと
  
  
